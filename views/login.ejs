<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - ApliKurir</title>
    <link rel="icon" href="img/logo-icon.png" type="image/x-icon">
    <link rel="stylesheet" href="style/login-style.css">
    <link rel="stylesheet" href="style/custom-style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="script/admin-script.js"></script>
</head>

<body>
    <form action="/login" class="form-login" method="post">
        <div class="logo">
            <img src="img/logo.png" alt="Logo_Perusahaan">
        </div>
        <div class="input-container">
            <label for="user-type">Status:</label>
            <select id="user-type" name="status">
                <option value="admin">Admin</option>
                <option value="agen">Agen</option>
                <option value="pegawai">Pegawai Gudang</option>
            </select>
        </div>
        <div class="input-container">
            <input type="email" name="email" placeholder="Email" value="<%= user != null ? user.email : "" %>" required />
        </div>
        <div class="input-container">
            <input type="password" id="password" name="password" placeholder="Password" value="<%= user != null ? user.password : "" %>" required />
            <span class="toggle-password" onclick="togglePassword()">
                <i class="far fa-eye"></i>
            </span>
        </div>
        <% if (message) { %>
            <div class="alert-message">
                <%= message %>
            </div>
        <% } %>
        <button class="green-btn" type="submit">Masuk</button>
    </form>
</body>

</html>
